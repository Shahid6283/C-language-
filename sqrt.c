#include<stdio.h>
#include<math.h>

// Find the Square root of the given number 

int main()
{
    float a,c;

    printf("Enter the Value : ");
    scanf("%f",&a);

    c = sqrt(a);

    printf("\nThe Value of Square root is : %f",c);

    return 0;
}